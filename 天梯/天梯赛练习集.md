## 1-001 Hello World

基本格式（不动脑子直接写）

```cpp
// 代码如下
#include<iostream>
using namespace std;
int main() { 
    cout<<"Hello World!"<<endl;
    return 0;
} 
```



## L1-012 计算指数

不符合题目要求就返回1

```cpp
if(...) return 1;
```

指数表示方法

```
特殊情况：2的n次幂，用位运算 << 。如`1<<3`，就是1乘2的3次幂；`1<<n`，就是1乘2的n次幂
通用方法：库函数pow( , )，默认返回double类型。如`pow(3, 10)`，就是3的10次幂。记得加头文件#include<cmath>
```



## L1-024 后天是星期几

当前星期为D，那么后天就是`D + 2`，很简单对不对？

但是要考虑特殊情况！如果D = 6，那么得到的结果是8，显然不对，应该是1；D = 7 同理

所以应该变成`(D + 2) % 7`！

改动之后，6、7符合要求了。这时候一定要记得验算，看看除了6、7以外的其他情况有没有受影响？

有！D = 5 时，再 % 7 就变成了0。所以我们可以单独把这种情况列出来，强行让它的结果为7，到这里才算结束了~

```cpp
// 代码如下
#include<iostream>
using namespace std;
int main() {
    int D;
    cin>>D;
    int res = (D + 2) % 7;
    if (res == 0) cout<<7;
    else cout<<res;
    return 0;
}
```



## L1-026 I Love GPLT

涉及字符串的记得加头文件#include<string>

字符串长度str.length()

```cpp
// 代码如下
#include<iostream>
using namespace std;
int main() {
    string str = "...";
	for(int i = 0; i < str.length(); i++) {
        cout<<str[i]<<endl; // 每个字符一行
	}
	return 0;
}
```



## L1-42 日期格式化

输入：“mm-dd-yyyy”，月、日、年

输出：“yyyy-mm-dd”，年、月、日

用C的scanf格式更简单（cpp也兼容C的scanf语法）

```c
// scanf和printf格式
scanf("%d %d", &a, &b); // 加&
printf("%02d %04d\n", a, b); // 02表示一定是2位，不足2位用0补
```

```cpp
// 代码如下
#include<iostream>
#include<string> // 字符串，加头文件
using namespace std;
int main() {
    int m, d, y;
    scanf("%d-%d-%d", &m, &d, &y);
    printf("%04d-%02d-%02d\n", y, m, d);
    return 0;
}
```



## L1-051 打折

保留小数点后几位，用cout<<fixed<<setprecision(n)<<...的方法控制，记得加头文件#include<iomanip>

```cpp
// 代码如下
include<iostream>
#include<iomanip> // 控制小数位数，加头文件
using namespace std;
int main() {
    int m1, dis;
    cin>>m1>>dis;
    cout<<fixed<<setprecision(2)<<m1 * dis * 0.1<<endl; // 保留2位小数
    return 0;
}
```



## L1-007 数字转拼音

每个数字有一个对应的拼音，如何对应？很简单，用一个数组就行，string数组

然后我们对输入的数字num逐位转换，如何获取每一位的数字呢？感觉有点麻烦，不如转换成字符串！

这样就可以通过字符串方法num[i]获取每一位数字了；上限位数也可以通过长度得到，就是num.length()。这样就有很多可用的方法了！

当然这样会出现一个新问题：字符如何转数字呢？

原理：字符 '0' ~ '9' 的 ASCII 码是连续的，'0' = 48, '1' = 49, '2' = 50, ... '9' = 57。所以字符 - '0' = 数字

我们再把整个思路缕一下。首先输入一个string num，创建string数组`string pinyin[10] = {"...", "...", ...};`。如果第一位是'-'的话，先输出一个"fu "。然后for循环，每一位的字符转数字，`int digit = num[i] - '0';` `cout<<pinyin[digit];`，然后注意细节最后一位后面不加空格就行。over！

```cpp
// 代码如下
#include<iostream>
#include<string>
using namespace std;
int main() {
    string num; // 开始的时候写int也可以，后面发现需要依次提取每个字符，再回头来改成string；当然一开始就想到这个问题，直接确定用string更好
    cin>>num;
    string pinyin[10] = {"ling", "yi", "er", "san", "si", "wu", "liu", "qi", "ba", "jiu"}; // 数组用花括号，字符串用双引号，字符用单引号
    int start = 0;
    if (num[0] == '-') {
        cout<<"fu "; // 这里有个空格别忘了！
        start++;
    }
    for(int i = start; i < num.length(); i++) {
        cout<<pinyin[num[i] - '0'];
        if (i != num.length() - 1) cout<<" ";
    }
    return 0;
}
```



## L1-008 求整数段和

5个数字一行，就是 i % 5 == 0 的时候换行

每个数字占5个宽度，右对齐，就是cout<<setw(5)<<...; 记得加头文件#include<iomanip>

这题有很多细节要注意，换行什么的，到底哪里在写endl

```cpp
// 代码如下
#include<iostream>
#include<iomanip> // 控制输出宽度，加头文件
using namespace std;
int main() {
    int A, B, count = 0;
    cin>>A>>B;
    for(int i = 1; i <= B - A + 1; i++) {
        int num = A + i - 1;
        cout<<setw(5)<<num;
        count += num;
        if(i % 5 == 0 && i != B - A + 1) cout<<endl; // 这里注意一种特殊情况，最后一行刚好满5个数的时候，这个数完了换一行，下一行输出数字和是一定要换行的，那么这里就换了两行，不符合要求。所以我们把输出数字和的换行单独放在后面，这里只有每行行尾，且不是最后一行行尾，才换行！
    }
    cout<<endl<<"Sum = "<<count<<endl;
    return 0;
}
```



## L1-010 比较大小

输入三个数，要按序输出

这题我暂时没有什么更好的思路，如果要不停交换的话，赋值来赋值去可能会比较绕，所以我觉得可以直接固定成a<b<c

然后比较的过程中，把最小的那个数赋值给a，最大的那个数赋值给c，保证最后的排序固定就不那么绕了

```cpp
// 代码如下
#include<iostream>
using namespace std;
int main() {
    int a, b, c;
    cin>>a>>b>>c;
    if(b < a) swap(a, b);
    if(c < a) swap(c, a);
    if(c < b) swap(b, c);
    cout<<a<<"->"<<b<<"->"<<c<<endl;;
    return 0;
}
```



## L1-013 阶乘和

思路对了就挺简单的。阶乘和，每个阶乘独立计算然后加到总和上去

重点是每个阶乘怎么算？递归！我们可以命名一个函数fac用来求阶乘，那么`fac(n) = n * fac(n - 1);`，剩下就没啥了

```cpp
// 代码如下
#include<iostream>
using namespace std;
long fac(int n); // 记得先声明，声明时类型都带
int main() {
    int N;
    long S = 0;
    cin>>N;
    for(int i = 1; i <= N; i++) {
        S += fac(i);
    }
    cout<<S<<endl;
}
long fac(int n) { // 返回类型是long
    long f;
    if(n == 0 || n == 1) f = 1; // 这里是从1开始，为什么有0？当然是因为递归的时候下一层是n-1，当n=1的时候不就是0了
    else f = n * fac(n - 1); // 递归
    return(f);
}
```



## L1-018 大笨钟

输入是19：05，中间有个：，所以要定义一个字符变量接收它。或者嫌麻烦的话直接scanf固定格式，就不用单独接收：了

定义一个dang，每次敲dang下；然后分情况判断dang等于多少

和L1-007挺像的，定义一个start，从start开始转换，然后分情况讨论start具体等于多少

分情况讨论的话，如果只有两种情况，要么要么，可以考虑三元运算符！`if(条件) ? a : b`

```cpp
// 代码如下
#include<iostream>
using namespace std;
int main() {
    int h, m;
    scanf("%d:%d", &h, &m);
    if(h < 12 || (h == 12 && m == 0)) {
        printf("Only %02d:%02d.  Too early to Dang.", h, m);
    } else {
        int dang = (m == 0) ? (h - 12) : (h - 11);
        for(int i = 0; i < dang; i++) {
        	cout<<"Dang";
        }
        cout<<endl;
    }
    return 0;
}
```



## L1-028 判断素数

单独定义一个函数，用来判断是否为素数

特殊情况先说明，1不是，2是，偶数都不是。然后看能否被奇数整除，从3开始，到sqrt(num)为止，每次加2，记得加头文件cmath，不用声明一个什么flag，直接返回true或者false就行

```cpp
// 代码如下
#include<iostream>
#include<cmath>
using namespace std;
bool isPrime(int num);
int main() {
    int N;
    cin>>N;
    for(int i = 0; i < N; i++) {
        int num;
        cin>>num;
        if(isPrime(num)) cout<<"Yes"<<endl;
        else cout<<"No"<<endl;
    }
    return 0;
}
bool isPrime(int num) {
    if(num == 1) return false;
    if(num == 2) return true;
    if(num % 2 == 0) return false;
    for(int i = 3; i <= sqrt(num); i += 2) {
        if(num % i == 0) return false;
    }
    return true;
}
```



## L1-031 到底是不是太胖了

绝对值要用函数abs()，不能直接写||，记得加头文件#include<cmath>

```cpp
abs(a - b)
```



## L1-037 A除以B

除法嘛，注意一个问题就行：

两个int作除法会自动截断，所以如果想要结果为小数，一定要先转换，让至少其中一个为double

```cpp
cout<<fixed<<setpricision(2)<<double(A) / B<<endl; // 结果保留两位小数
```



## L1-041 寻找250

输入一串数字，找第一个250。可以让他一直输入，我们一直接收，直至等于250为止。把输入以及等于250作为条件放到循环中

```cpp
while(cin>>num && num != 250) i++;
```

输入也可以作为条件放到循环中吗？

可以的，适用于一次输入多组数据，直到满足某个条件，就需要把 cin 和条件放到 while 循环里



## L1-055 谁是赢家

这题其实不复杂，只是东西很多，看起来很复杂。所以定义变量，见名知意很重要

a, b 两个人都有观众票和评委票，观众票题目定义了，分别为 Pa, Pb，只用比大小就行

评委票一共三票，0代表a，1代表b。我的建议是再定义两者的评委票数为P2a, P2b。评委票我们用数组P2[3]表示，然后遍历数组，是0的话P2a++，反之P2b++

```cpp
// 代码如下
#include<iostream>
using namespace std;
int main() {
    int Pa, Pb;
    cin>>Pa>>Pb;
    int P2[3];
    cin>>P2[0]>>P2[1]>>P2[2];
    int P2a = 0, P2b = 0;
    for(int i = 0; i < 3; i++) {
        if(P2[i] == 0) P2a++;
        else P2b++;
    }
    char P1more = (Pa > Pb) ? 'a' : 'b';
    if(P1more == 'a' && P2a >= 1) cout<<"The winner is a: "<<Pa<<" + "<<P2a<<endl;
    else cout<<"The winner is b: "<<Pb<<" + "<<P2b<<endl;
    return 0;
}
```



## L1-063 吃鱼还是吃肉

0代表女孩，1代表男孩；每种情况下又要分别判断身高、体重是太小、正好还是太大，情况非常多

我们可以考虑用数组，一个身高数组一个体重数组。女孩对应的数据就存在数组下标0的位置，男孩的存在数组下标1的位置，这里刚好是0和1，不浪费空间。记得在循环外面定义数组，避免每次循环都分配空间

```cpp
// 用数组
#include<iostream>
using namespace std;
int main() {
    int N;
    cin>>N;
    int sex, h, w;
    int H[2] = {129, 130}, W[2] = {25, 27}; // 在循环外面定义数组
    for(int i = 0; i < N; i++) {
        cin>>sex>>h>>w;
        if(h == H[sex]) cout<<"wan mei! ";
        else if(h > H[sex]) cout<<"ni li hai! ";
        else cout<<"duo chi yu! ";
        if(w == W[sex]) cout<<"wan mei!";
        else if(w > W[sex]) cout<<"shao chi rou!";
        else cout<<"duo chi rou!";
        cout<<endl;
    }
    return 0;
}
```

有个问题就是for循环里面重复代码很多，每种情况都要单独讨论，但是代码又是高度相似的，情况变多了的话代码就会非常冗余，可读性很差。数组存储量变大这是必然的，但是for循环这块应该还是可以改进的，我们用结构体试一下：

```cpp
// 用结构体
#include<iostream>
#include<string>
using namespace std;

struct Metric {
    int std[2];
    const string low, normal, high;
    void check(int sex, int val) {
        int s = std[sex];
        if(val < s) cout<<low;
        else if(val == s) cout<<normal;
        else cout<<high;
    }
}; // 结构体末尾记得加分号

int main() {
    int N, sex, h, w;
    cin>>N;
    
    Metric height = {{129, 130}, "duo chi yu! ", "wan mei! ", "ni li hai! "};
    Metric weight = {{25, 27}, "duo chi rou!", "wan mei!", "shao chi rou!"};
    
    while(N--) { // 先判断N是否为真，再N减1
        cin>>sex>>h>>w;
        height.check(sex, h);
        weight.check(sex, w);
        cout<<endl;
    }
    return 0;
}
```

问题解决~



## L1-005 考试座位号

最后是以试机座位号来查考试座位号，准考证号也可以和试机座位号关联起来，所以干脆都以试机座位号为标准。另两个都用数组存储，下标就是试机座位号

注意命名不要冲突了，比如准考证号的数组不要和它的变量名重复

```cpp
// 代码如下
#include<iostream>
using namespace std;
int main() {
    int arr_ks[1001]; // 注意是1001，座位号从1到1000，直接对应数组下标，所以数组就是0到1000，一共1001个
    string arr_zk[1001];
    int N;
    cin>>N;
    for(int i = 0; i < N; i++) {
        int sj, ks;
        string zk;
        cin>>zk>>sj>>ks;
        arr_zk[sj] = zk;
        arr_ks[sj] = ks;
    }
    int M, s;
    cin>>M;
    for(int i = 0; i < M; i++) {
        cin>>s;
        cout<<arr_zk[s]<<" "<<arr_ks[s]<<endl;
    }
    return 0;
}
```



## L1-015 跟奥巴马一起画方块

这题实际上就是考如何得到四舍五入后的整数？

比较取巧的一个方法是，a/b，当b为偶数时，可以通过 `(a + b/2) / 2` 得到结果

通用的方法是 `round()` 函数，记得加头文件#include<cmath>

```cpp
int rows = round(N / 2.0);  // 一定要有一个是double类型
```



## L1-017 到底有多二

这题本来感觉没什么可写的，但是改了好几次都还是错的😄

讲一下错误的点都在哪吧：

1. 多种情况间的关系理解的有问题。负数就是多少倍，偶数就是多少倍，这两个条件之间显然是并列的关系，不存在 else if 的关系，不要看到多个条件就 else if，else 了。而且题目还说了既是负数又是偶数就是两种的叠加，所以是两个独立的 if

2. 没换算成百分数。换算很简单，直接 * 100 就行

   在哪里 * 100 ？我先是在负数那里 * 100，然后发现偶数就很难判断要不要再 * 100，又要分情况，所以应该把 * 100 单独拿出来。要么放在最开始，算完比值马上 percent = ratio * 100，要么放在结尾 cout 那里，总之就是统一拿出来，不要放在条件里

3. 计算比值的时候记得分子先转成 double
4. 数组越界。最后一位下标应该是 N.length() - 1，因为数组从0开始
5. 位数不算负号。如果是负数的话，负号不应该算在位数内，此时位数应该是 N.length() - 1  

```cpp
// 代码如下
#include<iostream>
#include<iomanip>
using namespace std;
int main() {
    string N;
    cin>>N;
    double ratio, percent;
    int sum = 0;
    // 统计位数
    int len = N.length();
    if(N[0] == '-') {
        len -= 1;
    } // 负数的话，负号不能算在位数内！
    // 统计2的个数
    for(int i = 0; i < N.length(); i++) {
        if(N[i] == '2') sum++; // 记得2加单引号，字符比较
    }
    // 计算比值
    ratio = double(sum) / len; // 记得double
    // 换算成百分数
    percent = ratio * 100;
    // 如果是负数，增加0.5倍
    if(N[0] == '-') {
        percent *= 1.5;
    }
    // 如果是偶数，增加1倍
    if((N[N.length() - 1] - '0') % 2 == 0) {
        percent *= 2;
    }
    cout<<fixed<<setprecision(2)<<percent<<"%"<<endl;
    return 0;
}
```



## L1-025 正整数A+B

这题主要要解决的有两点：

1. 找到第一个空格，分割 A、B

   ```cpp
   // 如何找到第一个空格？
   // 我们可以把整个输入作为一个字符串，然后用字符串的find方法找到第一个空格的位置，再分割成A、B两部分
   
   string line;
   getline(cin, line); 
   // 找到第一个空格
   int pos = line.find(' ');
   string A = line.substr(0, pos); // A是空格前的部分
   string B = line.substr(pos + 1); // B是空格后的部分（到结尾）
   ```

2. 验证 A、B 是否是 [1, 1000] 的正整数

   ```cpp
   // 定义一个isvalid函数，用来验证是否是正整数
   bool isvalid(string s) {
       // 首先看每一位是不是都是数字
       for(int i = 0; i < s.length(); i++) {
           if(s[i] < '0' || s[i] > '9') {
               return false;
           }
       }
       // 首位不能为0
       if(s[0] == '0') return false;
       // 检查范围
       int num = 0;
       for(int i = 0; i < s.length(); i++) {
           num = num * 10 + (s[i] - '0'); // 当前的 num * 10 再加上新的个位数字，得到新num
       }
       return num >= 1 && num <= 1000;
   }
   ```

记得字符串 A、B 作加法的时候要先转换成数字，不然就是直接拼接字符串了

单个字符可以用 `c - ‘0’`，字符相减。但这里是字符串，不能减字符，所以要用 `stoi()` 方法，这个方法就是专门用来将字符串转化为整数的~



## L1-030 一帮一

首先这题肯定要用双指针。但是如果不匹配的话能不能直接 r-- 呢？不行，这样没匹配上永远被跳过，就组不上了

所以我们要考虑用队列。每次都从队尾往前扫，要和当前的队头元素匹配，然后匹配上的队头元素出队

只有对头元素出队够吗？试想，如果匹配上的低位元素不出队，那下一次匹配还会扫到它，有可能重复匹配，为了避免这个问题还需要标记一下已经匹配上的。那不如匹配上的两个都一起删了

但标准队列是无法实现的，因为它只能从队头出队，无法删除低位的；而且不能随机访问，无法从队尾往前扫

所以我们用**动态数组**！它能随机访问，还能通过 erase() 删掉任意位置的元素

```
// 动态数组用法
头文件 #include<vector> 
定义 vector<T> t;
用类似队列的 t.push_back() 方法添加将读取的元素添加到数组末尾
双指针找匹配的一对，匹配上的用 t.erase(t.begin() + r); 和 t.erase(t.begin() + l); 删掉
```

```cpp
// 代码如下
#include<iostream>
#include<vector>
#include<string>
using namespace std;

// 结构体
struct Student{
    int sex;
    string name;  
};

int main() {
    int N;
    cin>>N;
    // 创建动态数组，类型为Student
    vector<Student> stu;
    // 读入学生添加到数组末尾
    for(int i = 0; i < N; i++) {
        Student s;
        cin>>s.sex>>s.name;
        stu.push_back(s);
    }
    while(!stu.empty()) {
        int l = 0, r = stu.size() - 1; // 双指针
        // 不匹配就继续往前找
        while (stu[l].sex == stu[r].sex) {
            r--;  
        }
        // 直到匹配，输出
        cout<<stu[l].name<<" "<<stu[r].name<<endl;
        // 删掉匹配上的两个人
        // 必须先删后面的r，再删前面的l。如果先删l，后面的元素会前移，r的位置就变了
        stu.erase(stu.begin() + r);
        stu.erase(stu.begin() + l);
    }
    return 0;
}
```

细节：每个对象有多个性质的，可以用**结构体**封装起来



## L1-033 出生年

这题知识点就一个，集合 set 自动去重

要统计不同数字的个数，直接丢进集合里，就会自动去重啦

```cpp
bool isvalid(string y, int n) {
    set<char> digits; // 集合自动去重
    for(char c : y) {
        digits.insert(c); // 加入集合
    }
    return digits.size() == n; // 是否有n个不同数字
}
```





